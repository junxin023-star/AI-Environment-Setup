# AI Environment Setup
这是我作为机械专业学生，开启 AI 研究的第一步。

## 环境参数
- GPU: NVIDIA GeForce RTX 5060
- Python: 3.9 (Conda)
- Framework: PyTorch with CUDA support

## 验证脚本
运行 `import torch print(torch.cuda.is_available())` 返回 `True`。